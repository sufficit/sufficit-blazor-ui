@* Skeleton Loader Component - Loading State Pattern *@
@namespace Sufficit.Blazor.UI.Components

<div class="skeleton-loader @Class">
    @if (Type == SufSkeletonType.Card)
    {
        <div class="skeleton-card"></div>
    }
    else if (Type == SufSkeletonType.Text)
    {
        <div class="skeleton-text"></div>
    }
    else if (Type == SufSkeletonType.Circle)
    {
        <div class="skeleton-circle" style="width: @Size; height: @Size;"></div>
    }
    else if (Type == SufSkeletonType.Table)
    {
        @for (int i = 0; i < Rows; i++)
        {
            <div class="skeleton-text" style="margin-bottom: 0.75rem;"></div>
        }
    }
</div>

@code {
    [Parameter]
    public SufSkeletonType Type { get; set; } = SufSkeletonType.Text;

    [Parameter]
    public int Rows { get; set; } = 3;

    [Parameter]
    public string Size { get; set; } = "48px";

    [Parameter]
    public string? Class { get; set; }
}
